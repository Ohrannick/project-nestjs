<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <title>App</title>
</head>

<body>
  <h1>Наши сообщения</h1>
  <ul>
    {{#each messages}}
    <li>
      <h2>{{this.message}}</h2>
      {{#if this.author}}
      {{this.author}}
      {{else}}
      Автора нет
      {{/if}}
    </li>
    {{/each}}
  </ul>

</body>

</html>